{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make -j",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}