name: Build Static

on:
    workflow_dispatch:

# test with: act -W '.github/workflows/build-static.yml'
jobs:
    build_windows:
      uses: ./.github/workflows/build-static-win.yml

    build_mac:
      uses: ./.github/workflows/build-static-mac.yml

    build_linux:
      uses: ./.github/workflows/build-static-linux.yml
