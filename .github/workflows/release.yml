# Important information: https://intuit.github.io/auto/docs/build-platforms/github-actions

name: Release on NPM

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Branch to release from'
        required: true
        default: 'master'
        type: choice
        options:
          - master
          - beta
          - alpha
      dry_run:
        description: 'Dry run (no publish)'
        required: false
        default: false
        type: boolean

jobs:
  release:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository under ./libMvrGdtf
        uses: actions/checkout@v4
        with:
          path: libMvrGdtf
          ref: ${{ inputs.ref }}
          fetch-depth: 0
          fetch-tags: true
          token: ${{ secrets.ADMIN_TOKEN }}

      - name: Set up branch tracking
        working-directory: libMvrGdtf
        run: |
          git checkout ${{ inputs.ref }}
          git branch --set-upstream-to=origin/${{ inputs.ref }} ${{ inputs.ref }}
          git pull --ff-only
          echo "=== branch ==="
          git branch --show-current
          echo "=== status ==="
          git status -sb

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://npm.pkg.github.com/
          scope: '@mvrdevelopment'

      - name: Build
        uses: ./libMvrGdtf/.github/actions/build-lib

      - name: Create Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        working-directory: libMvrGdtf
        run: |
          npm install
          git config user.email "auto-release-bot@mvrdevelopment.com"
          git config user.name "Auto release bot"

          # auto's env-ci reads the branch from GITHUB_REF. On a
          # workflow_dispatch GITHUB_REF is always the branch the workflow was
          # launched from (master), NOT the ref we checked out. Writing it to
          # $GITHUB_ENV in a prior step does NOT override the runner's built-in
          # GITHUB_REF, so we export it inline here, right before calling auto.
          export GITHUB_REF="refs/heads/${{ inputs.ref }}"
          export GITHUB_REF_NAME="${{ inputs.ref }}"
          echo "Using GITHUB_REF=$GITHUB_REF"

          if [ "${{ inputs.dry_run }}" = "true" ]; then
            npx auto shipit --dry-run -vv
          else
            npx auto shipit -vv
          fi